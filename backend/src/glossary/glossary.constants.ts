export const glossarySwaggerDescriptions = {
  localeQuery: 'Locale code (e.g. es, en). Falls back to en if unsupported.',
  glossaryByLocale: 'Glossary categories and terms in the requested locale.',
} as const;
