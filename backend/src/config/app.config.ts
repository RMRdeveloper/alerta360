export const paginationConfig = {
  defaultPageSize: 15,
  maxPageSize: 100,
};

export const uploadConfig = {
  maxPhotosPerPost: 5,
};

export const rewardCurrencies = [
  'USD',
  'EUR',
  'MXN',
  'COP',
  'PEN',
  'DOP',
] as const;
